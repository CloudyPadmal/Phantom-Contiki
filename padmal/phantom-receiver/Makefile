CONTIKI_PROJECT = phantom-receiver

PLATFORMS_ONLY = sky

all: $(CONTIKI_PROJECT)

MAKE_MAC = MAKE_MAC_NULLMAC_SKY
MAKE_NET = MAKE_NET_NULLNET
MAKE_ROUTING = MAKE_ROUTING_NULLROUTING

CONTIKI = ../..
include $(CONTIKI)/Makefile.include